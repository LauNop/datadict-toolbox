import os
import Extract

"""
with open(".env", "r") as f:
    for line in f.readlines():
        try:
            key, value = line.split('=')
            os.putenv(key, value)
        except ValueError:
            # syntax error
            pass
"""

if __name__ == "__main__":
    print("Hello World")
